int countMonobit(int n) {
    if (n < 0) return 0;
    
    
    int count = 1;
    long current_ones = 1; 

    while (current_ones <= n) {
        count++;
        current_ones = (current_ones << 1) | 1;
    }

    return count;
}
